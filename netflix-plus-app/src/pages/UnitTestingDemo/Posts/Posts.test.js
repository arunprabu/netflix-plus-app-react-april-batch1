// AAA
import { render, screen } from '@testing-library/react';
import Posts from './Posts';

describe('Posts', () => {
  it('[SPYING]: renders posts properly', () => {
    render(<Posts />);
    // AA
  });
});
